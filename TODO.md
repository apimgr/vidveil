[ ] fix search results pagination, add buttons for prev/next
[ ] fix  infinite scroll not working i user enables via preferences
[ ] fix server not showing results with preview first is user sets it via preferences
[ ] fix the results count so it shows total results
[ ] fix the results so it shows in human readable format. ie: seconds/milliseconds
[ ] fix server/JS so it loads and uses what the user has their preferences to
[ ] fix the favorites not loading when user has favorites saved

