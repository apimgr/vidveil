# Repo-local Task History

This file was migrated from session-local SQL/checkpoint tracking so the ongoing spec-compliance audit survives the dev-server migration.

## Current Status

- Repo-local migration is complete.
- AI.md was refreshed from `~/Documents/templates/go/AI.md` on 2026-04-28 (≈5.6k insertions, ≈2.6k deletions vs prior copy). Project may need a fresh compliance sweep against the updated PART text.
- Two verified follow-up items remain open: the previously tracked client elevated-user rule, and a fresh post-template-refresh audit.

## Active / Pending

- [ ] client-elevated-user-rule — AI.md PART 33 says `vidveil-cli` always runs as a normal user and never as root/administrator, but the current client has no elevated-user startup guard yet. Do not implement this until the exact runtime behavior is pinned down against the existing cross-platform privilege helpers and the containerized validation flow.
- [ ] post-template-refresh-audit — AI.md was refreshed on 2026-04-28 from the canonical Go template. Re-read the PARTs touched by the refresh (especially 0, 1, 2, 3, 4, 5, 12, 14, 16, 17, 33) and run a focused compliance sweep on areas where the new spec language differs from the prior copy. Fix only verified drift; do not invent unrequested changes.

## Completed Migrated Tasks

- [x] bootstrap-commit-mess — Sync commit message — Reconcile .git/COMMIT_MESS with the actual uncommitted bootstrap changes after creating the missing rule files.
- [x] bootstrap-review — Review bootstrap state — Confirm current AI.md/TODO.AI.md/.claude state and the required rule-file outputs for this repo bootstrap.
- [x] bootstrap-rules — Create rule cheatsheets — Create the full .claude/rules/*.md cheatsheet set from the current AI.md PART mappings and repo-specific values.
- [x] audit-baseline — Run baseline checks — Run the existing repository build and test commands to establish the current baseline before making audit fixes.
- [x] audit-code-review — Inspect code compliance — Audit code, docs, and infrastructure against AI.md and identify concrete mismatches that require fixes.
- [x] audit-final-verify — Verify audit state — Re-run the existing build/test workflow and confirm the audited project state after fixes.
- [x] audit-fixes — Apply audit fixes — Fix all concrete spec mismatches found during the audit and keep documentation and infra in sync.
- [x] audit-spec-read — Read audit spec — Read AI.md PART 0 and PART 1 audit and compliance sections plus any directly relevant rule cheatsheets before auditing.
- [x] audit-followup — Run follow-up audit sweep — Re-read AI.md PART 0 and PART 1 doc-related requirements, inspect remaining modified files and untracked .claude/rules, fix any remaining spec drift, then refresh COMMIT_MESS to match the final worktree.
- [x] followup-verify — Verify post-audit state — Run the repository build/test commands after the final documentation and spec cleanup, then refresh COMMIT_MESS to match the final worktree.
- [x] final-drift-sweep — Run final drift sweep — Search the repository for any remaining spec drift after the documentation URL normalization, fix concrete issues found, then refresh COMMIT_MESS if needed.
- [x] docs-drift-sweep-2 — Sweep remaining docs drift — Search README.md, docs/, mkdocs.yml, and adjacent project docs for remaining relative public/admin/API URLs, stale path examples, or non-standard curl usage after the prior compliance fixes. Fix concrete issues found and then update COMMIT_MESS if scope changes.
- [x] metadata-sweep — Sweep metadata and badges — Inspect workflow files, README badges, docs deployment metadata, and unresolved placeholders outside the already-fixed docs set. Fix any concrete spec drift and refresh COMMIT_MESS if needed.
- [x] support-docs-sweep — Sweep support docs — Inspect root markdown/support files outside the already-audited docs set for stale URLs, placeholders, non-standard curl usage, and path drift. Fix concrete issues found and refresh COMMIT_MESS if scope changes.
- [x] privacy-copy-verify — Verify privacy copy changes — Run the repository build/test workflow after updating the public footer template and preferences documentation, then refresh COMMIT_MESS to include the privacy copy fix.
- [x] privacy-spec-recheck — Recheck privacy copy against spec — Re-read the relevant AI.md privacy/cookies/spec sections after the footer and preferences copy changes, confirm they still comply, and fix any conflict introduced by the latest edits.
- [x] user-guide-parity — Check user guide parity — Compare docs/user-guide/*.md against implemented search, bangs, and preferences behavior plus relevant AI.md guidance. Fix any concrete mismatches and refresh COMMIT_MESS if scope changes.
- [x] cli-guide-parity — Check CLI guide parity — Re-read the relevant AI.md documentation URL rules and compare docs/cli.md with the implemented vidveil-cli commands, flags, storage paths, and behavior. Fix concrete mismatches and refresh COMMIT_MESS if scope changes.
- [x] development-docs-parity — Check development docs parity — Re-read the relevant AI.md development/build/documentation rules and compare development docs, README development section, and Makefile-backed workflows against the current implementation. Fix concrete mismatches and refresh COMMIT_MESS if scope changes.
- [x] build-docs-sweep — Sweep remaining build docs — Re-read relevant AI.md build/development rules and search README.md plus docs/ for leftover stale development guidance such as host-Go instructions, nonexistent Make targets, stale repo structure, or root Dockerfile references. Fix concrete mismatches and refresh COMMIT_MESS if scope changes.
- [x] test-scripts-parity — Check test script parity — Re-read AI.md testing/development rules and compare tests/docker.sh and tests/incus.sh with the current Makefile/API behavior. Fix concrete stale assumptions in the scripts and rerun the supported validation commands.
- [x] ci-spec-audit — Audit CI and Docker — Re-read AI.md build/CI/Docker rules and compare workflow files plus docker assets against the current spec. Fix only concrete drift and re-run the supported validation commands.
- [x] automation-drift-sweep — Sweep remaining automation drift — Re-read AI.md automation/build rules and audit remaining automation and container support files such as Jenkinsfile and docker compose files for concrete spec drift. Fix only verified mismatches and rerun supported validation commands.
- [x] final-repo-drift-sweep — Sweep remaining repo drift — Re-read spec/rules for broad repo compliance and search the repository for leftover stale strings, paths, endpoints, naming, and automation mismatches. Fix only concrete verified drift and rerun supported validation commands if code or scripts change.
- [x] tracked-files-sweep — Sweep tracked files — Search tracked repo files excluding AI.md for remaining concrete spec drift in URLs, artifact names, stale metadata, deprecated endpoints, and outdated examples. Fix only verified mismatches and rerun supported validation if code or automation changes.
- [x] placeholder-sweep — Sweep leftover placeholders — Search tracked repo files for remaining concrete user-facing placeholder drift such as stale domain examples, outdated endpoint strings, or other mismatches that survived prior sweeps. Fix only verified user-facing drift and rerun supported validation if non-doc files change.
- [x] debug-docs-sweep — Verify debug docs — Check remaining troubleshooting and debug examples such as localhost:8080, pprof, and reverse proxy snippets against the actual server behavior and spec. Fix only concrete mismatches and rerun supported validation if non-doc files change.
- [x] exact-drift-sweep — Sweep exact remaining drift — Search tracked files for remaining exact-match spec drift in flags, ports, URLs, artifact names, placeholder domains, and user-facing examples. Fix only verified mismatches and rerun supported validation if code, templates, or automation files change.
- [x] command-docs-sweep — Sweep command docs — Search user-facing docs and templates for remaining exact command/flag/path mismatches such as nonexistent flags, wrong config semantics, stale ports, and outdated examples. Fix only verified drift and rerun supported validation if templates change.
- [x] command-surface-sweep — Sweep command surface — Search docs and templates for remaining vidveil command examples and compare them against the implemented server CLI/help output so any stale flags or subcommands can be corrected exactly.
- [x] backup-defaults-sweep — Verify backup defaults — Check backup guide defaults such as retention count and schedule against the actual config defaults and maintenance implementation, then fix any verified mismatches.
- [x] user-docs-exact-sweep — Sweep user docs exactly — Search README, docs, and public templates for remaining user-facing command examples, default values, and path claims that may drift from the implemented CLI/config behavior. Fix only exact verified mismatches.
- [x] defaults-sweep — Sweep documented defaults — Search README and docs for documented defaults, then compare those values against the actual config and CLI defaults in code. Fix only exact mismatches.
- [x] macos-service-path-fix — Fix macOS service naming drift — Align the launchd plist path and label in the wired service manager with AI.md PART 24/25 so service install, status, disable, and start/stop all use com.apimgr.vidveil naming.
- [x] cli-docs-sweep — Verify CLI doc defaults — Check docs/cli.md and README CLI wording against AI.md PART 33 and the actual vidveil-cli path, token, and precedence behavior. Fix only verified mismatches.
- [x] exact-sweep-next — Scan next exact drifts — Search repository docs and user-facing strings for the next verified spec or implementation mismatch after the defaults and CLI wording sweeps.
- [x] cli-flags-sweep — Verify documented CLI flags — Compare docs/cli.md command flag tables against the actual vidveil-cli subcommand parsers and fix only the verified mismatches.
- [x] probe-details-fix — Make probe output match docs — Expose search_time_ms in the CLI API client, show response time in probe output, and make --verbose emit meaningful extra per-engine details instead of being a no-op.
- [x] address-env-sweep — Verify listen env naming — Check AI.md, docs, entrypoint, and server CLI/env parsing for exact LISTEN vs ADDRESS behavior. Fix only verified drift.
- [x] remove-no-color-alias — Remove undocumented no-color alias — Read PART 8 and PART 33, remove the undocumented --no-color client alias so the CLI parser matches the documented/spec flag surface, then validate with the standard make targets.
- [x] sync-shell-completions — Sync client shell completions — Update generated client shell completions so bash, zsh, fish, and PowerShell advertise the actual supported commands and descriptions from the CLI help surface, then validate with the standard make targets.
- [x] cli-equals-flag-support — Support equals syntax in CLI flags — Audit global and command-specific client flag parsers against AI.md PART 33 flag syntax requirements, implement --flag=value support wherever the client currently only accepts space-separated values, then validate with make dev and make test.
- [x] client-shell-flag-alignment — Align client shell/help interface — Update the client to use spec-compliant --shell flag handling instead of a shell command, remove help/version command aliases, sync generated completions and docs to the flag-based interface, then validate with the standard make targets.
- [x] client-help-output-alignment — Align client help output — Update the client help text and matching CLI documentation so the --help output reflects the spec-required --shell completions/init/--help lines, correct usage/TUI wording, and the supported shell list.
- [x] client-token-env-alias — Accept legacy CLI token env alias — Handle the AI.md PART 33 token env-name split by keeping VIDVEIL_TOKEN canonical while accepting VIDVEIL_CLI_TOKEN as a fallback alias in the client, then update client-facing help/docs to document the compatibility behavior.
- [x] global-flags-after-command — Parse global flags after commands — Update the client global flag parser so documented global flags like --output, --server, --token, --color, and --timeout still work when they appear after a command, while preserving command-specific flags and command-level help behavior.
- [x] client-config-selection — Resolve and honor --config file selection — Implement the PART 33 --config resolution rules so the client resolves names and paths correctly, then use the resolved config path consistently for loading, login writes, setup-wizard writes, and visible help/output where appropriate.
- [x] client-token-file-path — Align default CLI token file path — Update the client default token file location from the CLI data directory to the PART 33 config directory path, then sync docs/help/tests so the default token file behavior matches the earlier detailed token-source and login sections of AI.md.
- [x] client-server-primary-persistence — Align CLI server config persistence — Teach the client to read legacy server.address but write spec-aligned server.primary, accept VIDVEIL_SERVER_PRIMARY with VIDVEIL_SERVER as a compatibility alias, and persist a valid --server value into the selected cli.yml when the config has no saved server yet.
- [x] client-token-config-key — Align CLI token config key — Treat top-level token in cli.yml as the canonical PART 33 config token source, continue reading legacy server.token for compatibility, and rewrite docs/tests so config-based token loading matches the earlier detailed token-source section.
- [x] client-env-precedence — Align CLI env precedence — Apply PART 33 env precedence so canonical env vars override config values, token-file beats env, and output/color/timeout/debug env mapping matches the documented client config priority.
- [x] client-official-site-default — Align CLI official site default — Embed the project official site into the client via CLI ldflags, use it as the PART 33 compiled default server when config/env are empty, remove the hardcoded API package fallback, and align help/first-run behavior with the official-site default.
- [x] client-config-autocreate — Auto-create cli.yml on first run — Update the client startup path so LoadCLIConfigFromFile creates the selected cli.yml when it is missing, using the current sane defaults before env and flag overrides, and add regression coverage plus docs wording for the first-run behavior.
- [x] client-setup-detection — Keep official site out of first-run detection — Adjust first-run setup detection so the compiled official-site default is used for runtime server resolution and help, but does not count as a saved server for the PART 33 setup wizard flow when cli.yml lacks a configured server URL.
- [x] build-official-site-source — Read official site from site.txt — Update the Makefile so OFFICIAL_SITE defaults from the spec-defined sources instead of empty, using OFFICIALSITE/OFFICIAL_SITE env overrides first and repo root site.txt next, so server and client binaries consistently embed the project official site.
- [x] client-tui-enabled-config — Honor tui.enabled config setting — Add the spec-defined tui.enabled setting to the client config, default it to true, and prevent automatic interactive TUI launch when a saved cli.yml sets tui.enabled: false. Keep the change limited to the TUI autostart path and sync docs/tests.
- [x] client-tui-shortcuts-help — Implement ? shortcuts help in TUI — Add the documented ? key behavior to the interactive TUI so it shows shortcuts help, keep the implementation small and visible in the TUI view, and add regression coverage for the key handling/rendered help text.
- [x] docs-tui-surface-alignment — Align TUI docs with implementation — Update docs/cli.md so the Interactive TUI Mode section and keyboard shortcuts reflect the current implemented TUI surface instead of advertising panel cycling, engine filtering, result preview, bang autocomplete, or SSE streaming that the current client TUI does not provide.
- [x] client-tui-config-shape — Align TUI config keys with PART 33 — Replace the legacy unused tui.show_hints config key with the PART 33 tui.mouse and tui.unicode keys in the client config model, auto-generated cli.yml, docs, and tests while keeping legacy show_hints readable for compatibility.
- [x] client-output-formats — Add YAML and CSV output formats — Implement the PART 33 output.format surface so search, engines, bangs, probe, and help/docs support yaml and csv in addition to json, table, and plain.
- [x] client-timeout-duration-config — Align CLI timeout config with PART 33 — Make cli.yml read and write server.timeout using the PART 33 duration form like 30s while preserving compatibility with existing integer timeout values and keeping flag/env timeout handling in seconds.
- [x] client-debug-config-key — Honor cli.yml debug setting — Add the PART 33 top-level debug config key to CLI config load/write behavior so generated cli.yml includes debug: false, config debug is honored, and env/flag precedence still wins over config.
- [x] client-api-version-config — Honor cli.yml api_version — Add the PART 33 server.api_version config key to the client config model and generated cli.yml, then thread it through the API client so search, version, health, engines, and probe requests use /api/{api_version}/ instead of a hardcoded /api/v1/.
- [x] client-admin-path-config — Add cli.yml server.admin_path key — Align the client config model and generated cli.yml with PART 33 by adding the missing server.admin_path key with the spec default admin, preserving existing behavior and updating docs/tests to match.
- [x] client-auth-config-shape — Align cli.yml auth block — Add spec-aligned auth.token and auth.token_file support to the client config, keep existing token precedence and compatibility reads for legacy token locations, write the canonical auth block into cli.yml, and update docs/tests to match.
- [x] client-output-config-shape — Align cli.yml output block — Add spec-aligned output.pager, output.quiet, and output.verbose keys to the client config and generated cli.yml, and apply output.verbose to the existing probe verbose behavior without inventing unsupported runtime behavior for pager or quiet.
- [x] client-logging-cache-config — Align cli.yml logging and cache blocks — Add the spec-defined logging and cache sections to the client config model and generated cli.yml with the documented defaults, keep the change shape-focused because PART 33 does not define more client runtime behavior here, and update docs/tests to match.
- [x] client-cli-log-init — Initialize cli.log on startup — Implement the PART 33 startup requirement to initialize cli.log after loading cli.yml and before command execution, using the configured logging.file override or the default client log path with 0600 permissions, and add regression coverage plus docs if needed.
- [x] client-startup-order — Move dir init after flag parsing — Align PART 33 startup order by moving EnsureClientDirs into ExecuteCLI after ParseCLIGlobalFlags and before config load/logging, and add regression coverage that ExecuteCLI creates the standard client directories during normal command startup.
- [x] client-config-permissions — Normalize cli.yml permissions on startup — Align PART 33 startup permissions by ensuring the selected cli.yml file is chmodded to 0600 after load/create, including pre-existing configs, and add regression coverage for an existing config with overly broad permissions.
- [x] client-ownership-verification — Verify client path ownership — Align PART 33 ownership verification by checking that standard client directories plus cli.yml and cli.log are owned by the running user after initialization, without inventing unrelated privilege-escalation behavior, and add regression coverage for the current-user ownership path.
- [x] client-token-file-permissions — Normalize default token file permissions — Align the default CLI token file with the same user-private permission and ownership guarantees by normalizing {config_dir}/token on startup when it exists and by reusing a shared write helper for login/setup token writes, with regression coverage for a pre-existing broad-permission token file.
- [x] repo-plan-todo-migration — Move plan and todo into repo — Create repo-local PLAN.AI.md and TODO.AI.md from the current session state so the ongoing spec-compliance audit history survives the dev-server migration.
