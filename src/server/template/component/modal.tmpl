{{/* Modal dialog component per AI.md PART 16 */}}
{{define "modal"}}
<dialog id="{{.ID}}" class="modal">
  <div class="modal-header">
    <h3 class="modal-title">{{.Title}}</h3>
    <button type="button" class="modal-close" data-action="close-dialog" aria-label="{{ t "a11y.close_dialog" }}">&times;</button>
  </div>
  <div class="modal-body">
    {{.Content}}
  </div>
  {{if .ShowFooter}}
  <div class="modal-footer">
    <button type="button" class="btn btn-secondary" data-action="close-dialog">{{ t "action.cancel" }}</button>
    <button type="button" class="btn btn-primary" id="{{.ID}}-confirm">{{ t "action.confirm" }}</button>
  </div>
  {{end}}
</dialog>
{{end}}
