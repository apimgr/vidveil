# Security

## Authentication

Configure at `/admin/security/auth`:

- Password requirements
- Session duration
- 2FA settings

## Rate Limiting

Configure at `/admin/security/ratelimit`:

- Requests per minute
- Bypass for trusted IPs

## Firewall

Configure at `/admin/security/firewall`:

- IP whitelist/blacklist
- Country blocking (with GeoIP)
